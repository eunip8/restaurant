<h1>Restaurants</h1>
<ul>
  <% @restaurants.each do |restaurant| %>
    <li><%= link_to restaurant.name, restaurant_path(restaurant.id) %> <%= restaurant.location %></li>
  <% end %>
</ul>


<%= link_to 'Post New Restaurant', new_restaurant_path %>
